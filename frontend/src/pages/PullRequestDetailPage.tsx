import { PagePlaceholder } from '@/components/PagePlaceholder';

export function PullRequestDetailPage() {
  return (
    <PagePlaceholder
      title="Pull request"
      description="A single pull request, its review jobs and their current state."
      milestone="Milestone 10"
    />
  );
}
