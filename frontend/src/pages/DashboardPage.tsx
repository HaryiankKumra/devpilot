import { PagePlaceholder } from '@/components/PagePlaceholder';

export function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="Recent pull request reviews across every connected repository, with risk scores at a glance."
      milestone="Milestone 10"
    />
  );
}
