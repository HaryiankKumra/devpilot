import { PagePlaceholder } from '@/components/PagePlaceholder';

export function LoginPage() {
  return (
    <PagePlaceholder
      title="Sign in"
      description="Authenticate with an email address and password to reach your dashboard."
      milestone="Milestone 2"
    />
  );
}
